metadata:
  name: k0s-hands-on
spec:
  hosts:
    - role: controller
      ssh:
        address: node-1
        user: node
        keyPath: ~/.ssh/id_rsa
        bastion:
          address: libvirt
          user: libvirt
    - role: worker
      ssh:
        address: node-2
        user: node
        keyPath: ~/.ssh/id_rsa
        bastion:
          address: libvirt
          user: libvirt
    - role: worker
      ssh:
        address: node-3
        user: node
        keyPath: ~/.ssh/id_rsa
        bastion:
          address: libvirt
          user: libvirt
